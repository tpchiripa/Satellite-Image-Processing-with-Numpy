"""
Area of Interest (AOI) representation: bounding box or polygon, with a
human-readable label (Phase 6). Deliberately generic/configurable per
project decision — no hardcoded region. Implemented starting Milestone 3,
when AOIs are first needed to scope a FIRMS query.
"""

from __future__ import annotations
